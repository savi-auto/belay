{
  "name": "belay",
  "version": "0.1.0",
  "description": "Cross-protocol position and liquidation-risk monitor for Stacks.",
  "type": "module",
  "private": true,
  "scripts": {
    "probe": "node scripts/probe.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "@stacks/transactions": "^7.2.0"
  }
}
